#pragma once
// TODO replace all bare `float` and `double` with following
// Don't use float or double explicitly
// and conversion from float to double takes time
#include <cstdint>
using dfloat_t = float;
using ddouble_t = double;
using dint = int32_t;
using duint = uint32_t;
using dbyte = uint8_t;
